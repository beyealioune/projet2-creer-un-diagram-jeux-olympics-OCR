export interface YearlyData {
    year: number;
    participations: number;
    medals: number; // Ajout du nombre de médailles par année
    athletes: number; // Ajout du nombre d'athlètes par année
  }